server.port=8083
spring.application.name=registration-service
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Forward Headers for Proxy
server.forward-headers-strategy=FRAMEWORK

# SpringDoc Prefixed Paths
springdoc.api-docs.path=/registration/v3/api-docs
springdoc.swagger-ui.path=/registration/swagger-ui.html
springdoc.swagger-ui.config-url=/registration/v3/api-docs/swagger-config
springdoc.swagger-ui.url=/registration/v3/api-docs

# Eureka & Razorpay & JWT
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
jwt.secret=${JWT_SECRET:9a4f4e35455ad3b246a4d538e78f99e82104323e4213a4f6789a44c1d2e3f4a5}
jwt.expiration=${JWT_EXPIRATION:86400000}
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SSO0IrU4HY77Xw}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:jAITj1WbUWt2wO4SXnrftkD2}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:vighnesh}

# Database - LOCAL (commented out to use NeonDB)
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://registration-db:5432/registration_db}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:user}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Database - NEONDB (active)
spring.datasource.url=jdbc:postgresql://ep-floral-moon-ad2xk7dc-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_VYbw2NX4SZPr

# Email / SMTP Configuration (uses ENV vars, falls back to Gmail defaults)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
